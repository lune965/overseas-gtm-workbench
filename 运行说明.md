# 🧭 出海罗盘 · GTM Compass — 项目运行说明

> **作品名称:** 出海罗盘 · GTM Compass
> **参赛:** 小红书外滩黑客松
> **技术栈:** Python 3 + Streamlit + Pandas + JSON
> **在线访问:** https://overseas-gtm-workbench-nj2sch7lmyxlkl4pt5ba8g.streamlit.app/

---

## 一、作品简介

**出海罗盘 · GTM Compass** 是一款面向中国消费电子品牌出海的售前与市场辅助工具。输入客户画像（目标市场 / 使用场景 / 核心诉求 / 预算），即可自动生成可落地的市场进入方案，覆盖**产品推荐、市场策略、社媒营销**三大环节，让一份 GTM 方案从"数天"压缩到"几分钟"。

### 三大功能模块

| 模块 | 功能 | 核心输出 |
|------|------|----------|
| 📋 方案匹配 | 市场×场景×诉求×预算 四维标签评分匹配 | Top3 产品组合 + 英文 Solution Brief（可导出） |
| 📈 市场策略 | 北美/欧洲/东南亚/中东 × 品类的差异化策略 | 渠道配比、定价区间、认证合规、本地化清单 |
| 📱 社媒营销 | 预热/发布/留存三阶段内容策略 | 英文帖文草稿、内容矩阵、发布 Checklist |

### 核心亮点
1. **四维画像 → 结构化方案**：把售前的隐性经验显性化，标签评分引擎替代人工判断
2. **品类深度联动**：品类贯穿产品推荐、渠道定价、社媒内容全流程
3. **内容生产自动化**：自动生成可编辑的英文社媒帖文，降低出海内容门槛

---

## 二、快速开始（30 秒体验）

无需安装任何环境，直接打开在线链接即可：

**https://overseas-gtm-workbench-nj2sch7lmyxlkl4pt5ba8g.streamlit.app/**

体验路径：
1. **侧边栏**设置客户画像：目标市场 → 使用场景 → 核心诉求 → 预算范围 → 产品品类
2. **Tab 1 方案匹配**：查看推荐的 Top3 产品组合与英文方案简报，可导出 Markdown
3. **Tab 2 市场策略**：查看该市场+品类的渠道配比、定价区间、本地化清单
4. **Tab 3 社媒营销**：选择营销阶段，查看内容矩阵、帖文草稿与发布 Checklist

---

## 三、本地运行

### 环境要求
- Python **3.10 及以上**（推荐 3.10/3.11）
- 依赖：`streamlit>=1.28`、`pandas>=1.5.0`

### 步骤

```bash
# 1. 进入项目目录
cd smart-home-presales-tool

# 2. （可选）创建虚拟环境
python -m venv venv
venv\Scripts\activate        # Windows
source venv/bin/activate     # macOS / Linux

# 3. 安装依赖
pip install -r requirements.txt

# 4. 启动应用
streamlit run app.py
```

启动后浏览器自动打开 **http://localhost:8501**（默认端口）。

> 也可指定端口：`streamlit run app.py --server.port 8502`

---

## 四、部署到公网（Streamlit Community Cloud）

### 前提
- 一个 GitHub 账号，代码已推送到仓库 `lune965/overseas-gtm-workbench`（分支 `main`）

### 部署步骤
1. 打开 **https://share.streamlit.io** 并用 GitHub 账号登录
2. 点击 **New app**
3. **Repository** 选择 `lune965/overseas-gtm-workbench`
4. **Branch** 选择 `main`
5. **Main file** 输入 `app.py`
6. **Advanced settings** 中建议将 **Python version** 选为 **3.10 或 3.11**（Streamlit Cloud 的 Python 3.14 默认环境存在已知兼容性问题，可能导致 TypeError）
7. 点击 **Deploy**，等待 1-3 分钟构建完成
8. 部署完成后会分配一个公网链接，形如 `https://<your-app-name>-xxxx.streamlit.app/`

### 后续更新代码
```bash
git add .
git commit -m "更新内容"
git push origin main
```
推送到 `main` 分支后，Streamlit Cloud 会自动重新部署。

### 常见问题排查
| 现象 | 处理方式 |
|------|----------|
| 部署后报 `TypeError` | 在 Streamlit Cloud 的 app 设置中确认 Python 版本为 3.10/3.11；或点 **Reboot** 强制重新构建 |
| 页面一直显示旧版 | 浏览器强制刷新（Ctrl+Shift+R）；或在 Cloud 后台点 **Clear cache / Reboot** |
| 推送代码失败 | 项目使用 SSH 认证（`git@github.com:...`），确认本机 SSH 公钥已添加到 GitHub 账号 |

---

## 五、项目结构

```
smart-home-presales-tool/
├── app.py                  # Streamlit 主应用（三 Tab 界面）
├── requirements.txt        # 依赖清单
├── engine/
│   ├── matcher.py          # 标签评分匹配引擎（含品类筛选）
│   ├── generator.py        # Solution Brief 生成与导出
│   └── __init__.py
├── data/
│   └── products.json       # 12 个消费电子产品数据（品类/标签）
├── docs/                   # 需求与开发文档
├── portfolio.html          # 作品集展示页
└── 运行说明.md              # 本文档
```

---

## 六、数据与版权说明

- 产品数据、市场策略、社媒内容均为**模拟示例数据**，用于演示工具能力
- 项目无外部 API 依赖，数据全部内置，可离线运行
- 界面中文、产品数据英文、方案简报英文，符合出海场景的真实使用习惯
